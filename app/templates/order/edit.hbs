{{#view 'upable' shouldShow=shouldShowCreate}}
  <div class='header'>
    <a class='left'></a>
    <h2 class='title'>{{content.name}}</h2>
    <a href='#' {{action 'back' target='view'}}>zurück</a>
  </div>
  <div class='content list'>
    {{partial 'order/form'}}
  </div>
{{/view}}
