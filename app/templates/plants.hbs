<div class="moosi">
  <div class='header'>
    <a class='left' {{action 'openActionSheet'}}>einstellungen</a>
    <h1 class="title">Pflanzen</h1>
    {{#link-to 'plants.create'}}neu{{/link-to}}
  </div>

  <div class='content list'>
    {{#if hasActiveOrder}}
      <div class="item active-order rows">
        <div class="row" {{bind-attr class="hasFrom:slim"}}>
          <span>
            {{#link-to 'order' activeOrder}}{{activeOrder.name}}{{/link-to}}
            <a href='#' {{action 'deselectActiveOrder'}} class="smaller">abbrechen</a>
          </span>


          <span class='flex'></span>
          <span class='smaller note'><em>wähle die Pflanze</em></span>
        </div>


      </div>
    {{/if}}
    {{#each plant in arrangedContent}}
      <div class="item" {{action 'linkToPlant' plant}}>
        <span>{{plant.name}}</span>
        <span class='flex'></span>
        <span class='number'>{{euro plant.price}}</span>
        <span class='number calm padding-left'>{{plant.youngPlantQuantity}}</span>
        <span class='number padding-left'>{{plant.quantity}}</span>
      </div>
    {{/each}}
  </div>
  {{view 'tabs'}}
</div>
{{outlet}}

{{#modal-box isModalVisible=modalVisible}}
    <h3>Modal</h3>
    <hr/>
    <p></p>

{{/modal-box}}


{{action-sheet isVisible=actionSheetVisible goToQuarters='goToQuarters' }}

