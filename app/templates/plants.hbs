<div class="moosi">
  <div class='header'>
    <h1 class="title">Pflanzen</h1>
    {{#link-to 'plants.create'}}add{{/link-to}}
  </div>

  <div class='content plant-list'>
    {{#each content}}
      <item class="item" {{action 'linkToPlant' this }}>{{name}}</item>
    {{/each}}
  </div>
</div>
{{outlet}}
<div>
  footer
</div
