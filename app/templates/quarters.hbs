{{#view 'upable'}}
    <div class='header'>
    <a class='left' {{action 'back' target='view'}}>back</a>
    <h2 class='title'>Quarters</h2>
    <a></a>
  </div>
  <div>
  <div class='content list'>
    {{#each quarter in content }}
      <div class="item">
        <span>{{quarter.name}}</span>
      </div>
    {{/each}}
  </div>

  <div class='padding flexbox'>
   {{input value=selectedQuarter.name}}
   <div class="aright">
     <button {{action 'createQuarter'}}>
        hinzufügen
     </button>
   </div>
  </div>
{{/view}}
